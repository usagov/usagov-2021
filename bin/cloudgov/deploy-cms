#!/usr/bin/env bash
#
# This script will attempt to deploy a cms app by tag
#

# we might be running in circleci
if [ -f /home/circleci/project/env.local ]; then
  . /home/circleci/project/env.local
fi
# we might be running from a local dev machine
SCRIPT_DIR="$(dirname "$(readlink -f "$0")")"
if [ -f $SCRIPT_DIR/env.local ]; then
  . $SCRIPT_DIR/env.local
fi
if [ -f ./env.local ]; then
  . ./env.local
fi

# If DRYRUN is set, don't run the commands, but echo them.
if [ -n "${DRYRUN}" ]; then
    output="echo # "
fi

DOCKERUSER=${DOCKERUSER:-gsatts}
DOCKERREPO=${DOCKERREPO:-usagov-2021}
CTAG=${1:-latest}
CDIGEST=${2:-}

APP_SPACE=$( cf target | grep space: | awk '{ print $2 }')
APP_ORG=$(   cf target | grep org:   | awk '{ print $2 }')

# The Image digest for this tag should be looked up from cloud.gov storage
# Any tag with a stored Image digest should be referenced by hash instead of tag

function appExists {  # appname
  app=$1
  if [ -z "$app" ]; then
    echo "ERROR: appname is required"
    exit 1
  fi
  echo cf app $app > /dev/null 2>&1
  cf app $app > /dev/null 2>&1
  return $?
}

function restartApp { #appname
  app=$1
  if [ -z "$app" ]; then
    echo "ERROR: appname is required"
    exit 1
  fi
  $output cf restart $app --no-wait --strategy rolling
}

function pushApp { #appname
  app=$1
  if [ -z "$app" ]; then
    echo "ERROR: appname is required"
    exit 1
  fi

  instances="-i 1"
  if [ "$APP_SPACE" == "prod" ]; then
    instances="-i 2"
  elif [ "$APP_SPACE" == "stage" ]; then
    instances="-i 1"
  elif [ "$APP_SPACE" == "dev" ]; then
    instances="-i 1"
  elif [ "$APP_SPACE" == "dev-dr" ]; then
    instances="-i 1"
    random_route="--random-route"
    newrelicspace="shared-egress-dr"
  fi

  if [ -z "$CDIGEST" ]; then
    echo "Deploying ${DOCKERUSER}/${DOCKERREPO}:${app}-${CTAG}"
    $output cf push $app $instances ${random_route} --docker-image ${DOCKERUSER}/${DOCKERREPO}:${app}-${CTAG}
  else
    echo "Deploying ${DOCKERUSER}/${DOCKERREPO} ${app}-${CTAG} via digest ${CDIGEST}"
    $output cf push $app $instances  ${random_route} --docker-image ${DOCKERUSER}/${DOCKERREPO}\@${CDIGEST}
  fi
}

function setupRoutes { #appname
  app=$1
  if [ -z "$app" ]; then
    echo "ERROR: appname is required"
    exit 1
  fi
  # the egress-setup doesn't interfere with bootstrapping
  # but cms needs to exist in the first place
  # for route mapping and egress setup to run

  $output cf create-route apps.internal --hostname cms-$APP_SPACE-usagov
  $output cf map-route $app apps.internal --hostname cms-$APP_SPACE-usagov
  $output cf create-route apps.internal --hostname beta-$APP_SPACE-usagov
  $output cf map-route $app apps.internal --hostname beta-$APP_SPACE-usagov

  if [ "$APP_SPACE" == "prod" ]; then
    $output cf create-route usa.gov --hostname cms
    $output cf map-route $app usa.gov --hostname cms
    $output cf create-route usa.gov --hostname beta
    $output cf map-route $app usa.gov --hostname beta

  else
    $output cf create-route usa.gov --hostname cms-$APP_SPACE
    $output cf map-route $app usa.gov --hostname cms-$APP_SPACE
    $output cf create-route usa.gov --hostname beta-$APP_SPACE
    $output cf map-route $app usa.gov --hostname beta-$APP_SPACE

  fi
}

function setupEgress { #appname
  app=$1
  if [ -z "$app" ]; then
    echo "ERROR: appname is required"
    exit 1
  fi
  $output $SCRIPT_DIR/setup-egress-for-apps $app --no-restart
}

function setupNewrelic { #appname
  app=$1
  space=$2
  if [ -z "$app" ]; then
    echo "ERROR: appname is required"
    exit 1
  fi
  $output $SCRIPT_DIR/setup-newrelic-for-apps $app --no-restart $space
}

if appExists cms; then
  setupRoutes cms
#  setupEgress cms
  setupNewrelic cms $newrelicspace
  pushApp cms
else
  cf set-env cms SKIP_DRUPAL_BOOTSTRAP "skip"
  pushApp cms
  setupRoutes cms
#  setupEgress cms
  setupNewrelic cms $newrelicspace
  cf set-env cms SKIP_DRUPAL_BOOTSTRAP ""
  restartApp cms
fi
