#!/bin/bash
set -e

USER=${1}
PASS=${2}

if [[ -n "$USER" ]] && [[  -n "$PASS" ]]; then
    cf login -a https://api.fr.cloud.gov -u $USER -p $PASS

else 
    cf login -a https://api.fr.cloud.gov --sso

fi
