#!/bin/bash
set -e

usage="
$0: SSH into specified app.

Usage:
   $0 -h
   $0 WHERE

Options:
-h:            show help and exit
WHERE:         app name (default: cms)
"

PREAMBLE=$( pwd -P )/bin/includes/preamble.sh
if [ -f "$PREAMBLE" ]; then
  # shellcheck source=bin/includes/preamble.sh
  . "$PREAMBLE"
else
   echo Cannot find preamble at "$PREAMBLE"
   exit 1
fi

WHERE=${1:-cms}

cf ssh "$WHERE"
