#!/bin/bash
set -e

# this should ssh into cloudgov and trigger bootstrap from there 
# docker compose run \
#   --rm \
#   --workdir /var/www \
#   -v $(pwd)/:/var/www:delegated \
#   cms \
#   /var/www/bootstrap.sh