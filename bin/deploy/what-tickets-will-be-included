#!/bin/sh

SCRIPT_DIR="$(dirname $(readlink -f "$0"))"
if [ -f $SCRIPT_DIR/includes ]; then
  . $SCRIPT_DIR/includes
fi

DEPLOY_BRANCH=${DEPLOY_BRANCH:-}
MERGE_BRANCH=${MERGE_BRANCH:-}
echo "Tickets in deployment:" | toLogs
bin/ccb $MERGE_BRANCH $DEPLOY_BRANCH | toLogs

return 0
