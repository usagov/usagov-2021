#!/bin/sh

SCRIPT_DIR="$(dirname $(readlink -f "$0"))"
if [ -f $SCRIPT_DIR/includes ]; then
  . $SCRIPT_DIR/includes
fi

assertRequires git

# Make sure git is up to date
echo "Merging git branches stage => prod" | toLogs

git checkout dan | toLogs
git merge dev | toLogs
git checkout dev | toLogs
git merge dan
