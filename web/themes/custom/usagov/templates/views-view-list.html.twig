{#
/**
 * @file
 * Default theme implementation for a view template to display a list of rows.
 *
 * Available variables:
 * - attributes: HTML attributes for the container.
 * - rows: A list of rows for this list.
 *   - attributes: The row's HTML attributes.
 *   - content: The row's contents.
 * - title: The title of this group of rows. May be empty.
 * - list: @todo.
 *   - type: Starting tag will be either a ul or ol.
 *   - attributes: HTML attributes for the list element.
 *
 * @see template_preprocess_views_view_list()
 *
 * @ingroup themeable
 */
#}
{% set is_directory = view.element['#name'] == 'federal_agencies' %}
{% if attributes %}
  <div{{ attributes }}>
{% endif %}
  {% if title %}
    <h3>{{ title }}</h3>
  {% endif %}
 
{% if is_directory %}
{#
 **************************************************    
 * This is USAgov-customized code for the federal agencies directory. 
 * See views-view-fields.html.twig for layout of an individual directory 
 * record within this loop. 
 **************************************************
#}

{% set current_letter = view.args[0]|upper %}
<h2 class="usagov-directory-letter-heading">{{- current_letter -}}</h2>
  {% for row in rows %}
  {{ row.content }}
  {% endfor %}
{% else %}
{#
 **************************************************
 * This is the default drupal layout for a view in "list" mode.
 **************************************************
#}
  <{{ list.type }}{{ list.attributes }}>
  {% for row in rows %}
  <li{{ row.attributes }}>
    {{ row.content }}
  </li> 
  {% endfor %}
  </{{ list.type }}>  
{% endif %}

{% if attributes %}
  </div>
{% endif %}
  
